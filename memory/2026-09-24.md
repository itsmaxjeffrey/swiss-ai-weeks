## viseca-shopper-ui cleanup + symlink incident (project: /home/coffee/.openclaw/workspace/swiss-ai-weeks)

<!-- project: /home/coffee/.openclaw/workspace/swiss-ai-weeks -->

- Owner: "trash the ones u do not need" (post-merge leftovers). Trashed via gio:
  old checkout `~/workspace/viseca-shopper-ui`, dead deploy key file
  `~/.ssh/github_deploy_viseca_shopper_ui`; removed its Host block from
  `~/.ssh/config`. Deploy key 164225718 fully retired locally; GitHub-side key
  dies with the repo.
- **Symlink incident**: at Sep 23 20:36 (after my merge) something replaced
  `swiss-ai-weeks/viseca-shopper-ui/` with a symlink → `../viseca-shopper-ui`
  (the old checkout). Trashing the old checkout broke it → git showed all UI
  files as unstaged-deleted + untracked symlink artifact. History never at
  risk (HEAD 511e98d has full subtree). Fix: trashed symlink, `git checkout --
  viseca-shopper-ui` restored real files, status clean. Origin: unknown — not
  any of my commands.
- UI server 8794 restarted from restored monorepo path; /api/health OK. It dies
  on gateway restarts — restart: `cd swiss-ai-weeks/viseca-shopper-ui && PORT=8794
  HOST=127.0.0.1 node server.js` (log /tmp/viseca-shopper-ui-8794.log).
- **GitHub repo `itsmaxjeffrey/viseca-shopper-ui` NOT yet deleted**: stored
  GITHUB_PAT now 401s (Bad credentials on /user — worked yesterday). Awaiting
  fresh token, then `DELETE /repos/itsmaxjeffrey/viseca-shopper-ui` (needs
  delete_repo scope; classic PAT or fine-grained w/ Administration rw).
- FYI: trash also contained BOOTSTRAP/HEARTBEAT/IDENTITY/SOUL/USER .md files I
  did not trash — owner or another session's cleanup; left alone.

## viseca-shopper: Impressum merchant-identity check (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner asked: shopper should check the Impressum of the site it buys from
(company, address, registry number if stated). Built in the **viseca-shopper
agent workspace repo** (~/.openclaw/workspace/viseca-shopper, first commit
`37c876c`):

- `scripts/impressum-check.js` — zero-dep Node: discovers Impressum page
  (homepage footer anchors → common paths), extracts company_name, legal_form,
  address {street, postal_code, city, country}, uid (CHE-x.x.x normalized),
  registry_number (HR-ID CH-x.x.x.x-x or labelled register number — only when
  actually printed). Line-anchored address-block parsing; 1 retry per candidate
  (shops soft-block intermittently); status: found/not_found/blocked.
- AGENTS.md section: agent runs the check before completing any purchase,
  reports the merchant identity block, falls back to web_fetch → browser on
  bot walls, surfaces missing Impressum as a trust signal. Never invent values.
- Live-verified: Denner AG / Grubenstrasse 10 / 8045 Zürich; TRISA AG /
  Kantonsstrasse 31 / 6234 Triengen; Ochsner Sport AG + HR-ID
  CH-020.3.047.067-8; Post CH AG + UID CHE-109.030.864. Denner/TRISA print no
  UID/registry — script says "not stated" (owner expected this).
- **Bot walls are real**: digitec/galaxus, brack, interdiscount, fust,
  microspot, aldi-suisse 403/timeout plain fetches. Script → status "blocked";
  agent instructions route those to web_fetch/browser fallback.
- Bugs hit while building: `baseUrl.origin` on a string (Invalid URL),
  contentScore line-anchored regexes without `m` flag (rejected good pages),
  multi-line STREET_RE capture (product junk like "P-6000 Herren Sneaker").

## viseca-shopper UI exposed at viseca-shopper.pixerful.com (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner: "create a domain for it... subdomain on my pixerful.com (just like u did
for other apps)". Live and verified: health JSON + HTTP 200 over public HTTPS.

- **Infra map**: pve host `192.168.1.100` (ssh root@ ok); caddy LXC
  `192.168.1.109` (ssh alias `caddy`, Caddy 2.11.4, `/etc/caddy/Caddyfile`, CF
  token in `/etc/caddy/cloudflare.env`); this VM = qemu **101** =
  `192.168.1.103` (MAC bc:24:11:a1:26:58); dns box `192.168.1.105`. DNS:
  Cloudflare NS, **wildcard `*.pixerful.com` → WAN 31.164.181.196** — new
  subdomains need NO DNS change, only Caddy + firewall.
- **Three firewall layers, all must pass**: (1) router NAT (443 → caddy only),
  (2) PVE cluster firewall `policy_in: DROP`, per-guest rules in
  `/etc/pve/firewall/<vmid>.fw` — added `IN ACCEPT -source +dc/management -p
  tcp -dport 8794` to `101.fw` (backup `.pre-viseca-20260924.bak`),
  (3) **guest ufw on this VM (active, silently drops unknown ports)** — owner
  ran `sudo ufw allow in on ens18 proto tcp from 192.168.1.109 to any port
  8794 comment 'Caddy to Viseca Shopper UI'` (pattern visible in
  `~/.bash_history`; every app has one).
- **No root on this VM**: no passwordless sudo, no root SSH key. Root work here
  goes through the owner.
- Caddy block `viseca-shopper.pixerful.com { reverse_proxy 192.168.1.103:8794
  }` appended (owner renamed from `viseca.` mid-flight); backup
  `Caddyfile.bak-20260924-viseca`; validate with `set -a; .
  /etc/caddy/cloudflare.env; set +a; caddy validate --config
  /etc/caddy/Caddyfile` then `systemctl reload caddy`. TLS auto-issues via
  CF DNS-01 on reload.
- UI server rebound `HOST=127.0.0.1` → **`HOST=0.0.0.0`**: restart with
  `cd swiss-ai-weeks/viseca-shopper-ui && setsid nohup env HOST=0.0.0.0
  PORT=8794 node server.js >> /tmp/viseca-shopper-ui-8794.log 2>&1 < /dev/null
  &` (setsid nohup, NOT exec background:true — see Sep 23 reaping lesson).
- **Diagnosis lesson**: silent SYN drop = capture ON THE PVE HOST *concurrently*
  with the client curl (my first tcpdump ran after the curls → "0 packets"
  red herring). Sequence seen: SYNs cross tap101i0 into guest, no SYN-ACK/RST
  back → guest-local drop (ufw), not PVE.
- Recipe for next app: wildcard DNS (nothing) + PVE rule + guest ufw rule +
  Caddy block + bind 0.0.0.0.

## Browser-bypass test on bot walls — verdict: not bypassable from this host (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner asked to try a real browser against the walls. Tried all three routes:
- Standalone `google-chrome --headless=new --dump-dom`: broken on this host even
  for example.com (40-byte empty DOM, zero stderr) — environmental, not site-side.
- OpenClaw managed browser (real headless Chrome, CDP, profile `openclaw`):
  works fine on normal sites, but digitec.ch RSTs at HTTP/2
  (ERR_HTTP2_PROTOCOL_ERROR) even on the homepage; brack.ch and fust.ch render
  empty/challenge bodies. `openclaw browser set` has no stealth/headed toggles.
- Headed Chrome via xvfb-run on digitec: 0 bytes produced.
Conclusion: digitec/galaxus runs DataDome-class TLS/HTTP2-fingerprint rejection
(server-side RST) — client-side tricks don't apply; datacenter IP reputation
also plays in. Agent instructions updated (commit `b88d485`): don't retry walled
shops via browser more than once, report bot-walled, offer registry
corroboration instead. Zefix needs a free registered API token (401 without) —
flagged as follow-up for the LEASH-native verification path.

## viseca-shopper UI: live turn progress via SSE (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner: "if there are any ongoing tasks, it should show it" — the UI only had a
generic spinner while multi-minute shopping turns ran. Built live progress
(commit `b28b8b9`):

- **Source evaluation**: `openclaw agent --json` buffers everything (0 bytes
  until final blob — no live events); gateway log tail is live but lines carry
  no session key (shared gateway = wrong-agent activity risk); transcripts are
  SQLite now, not tailable JSONL. Winner: `openclaw sessions --json --active 3
  --agent <id>` — per-session `status`/`totalTokens`/`updatedAt`, correctly
  attributed, token counts visibly tick during turns.
- **Bridge**: POST /api/chat now answers `text/event-stream`: `start` frame →
  `progress` frames every 4s (sessionSnapshot poll) → `done`/`error`. Turn
  ceiling raised 300s → 600s (a real browsing turn died at 300s; CLI default
  is 600 too). Client abort safety net 630s. Verified frames through caddy.
- **UI**: spinner line now renders real data: "browsing offers… · 2m13s ·
  44.3k tok", degrades to rotating words before first progress frame.
- **Concurrent-session collision**: while I edited server.js/app.js, another
  session added the Order Policy Gate (Ed25519 signing authority in the bridge,
  `/api/policy/sign` + `/api/policy/pubkey`, ```policy-json approval cards in
  the UI) to the SAME files — my first edit failed on stale oldText, which is
  what saved their work. Lesson: always re-read before editing repo files in
  this multi-agent workspace; commit messages should credit both changesets
  when files interleave.

## Order Policy Gate — signed, time-bound purchase policies (built & live) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

Owner asked: agent must fix a policy per request, get customer confirmation BEFORE shopping,
crypto so the agent can't change it, time bounds (deliver-by / order-by), and ask instead of
guessing when data is missing. Built across both repos, verified end-to-end:

- **`viseca-shopper/scripts/policy.js`** (agent repo, commit `950c9b9`, local-only repo): zero-dep
  Ed25519 policy tool — `template|validate|sign|verify|check|init-keys|selftest`. Canonical-JSON
  signing; authority REFUSES incomplete policies (exit 4 = the no-missing-data rule); time windows
  `timing.order_by` / `deliver_by` / `search_until` enforced (ISO offset required, Europe/Zurich
  display); exit codes 0 ok / 2 tampered / 3 window violated / 4 malformed. Selftest 11/11.
- **Key custody**: private key lives in `viseca-shopper-ui/keys/` (0600, gitignored) — OUTSIDE the
  agent workspace. Bridge seeds the public key into `viseca-shopper/keys/` on every boot; agent can
  verify, never forge. Honest caveat: same OS user, so the hard guarantee is *detectability* (any
  edit breaks the signature) + the signer refusing partial policies; the agent holds no signing path.
- **Bridge** (`3f7cd35`, pushed): `POST /api/policy/sign` validates + signs + files
  `policies/<policy_id>.signed.json` into the agent workspace; incomplete → HTTP 422 + missing list.
  `GET /api/policy/pubkey`. Parallel session's `b28b8b9` merged this with their SSE streaming work —
  both changesets verified present in current files, collision resolved without loss.
- **UI**: agent replies with a ```policy-json fenced block → renders as an approval card
  (items/budget/order-by/deliver-by/payment). Approve & Sign auto-tells the agent to run the gate;
  refusal surfaces exact missing fields so the agent must ask.
- **AGENTS.md** (agent repo): binding 8-step Order Policy Gate — draft only from customer input,
  validate, present, never sign/edit (changes = new policy_id), `policy.js check` before search /
  checkout / payment, deliver-by ≤ product promise, receipt to `policies/<id>.receipt.json`.
- **Live**: bridge restarted 12:28 from canonical path running combined code; authority fingerprint
  `56dbd0159f452b45`; verified sign → gate 0 → budget-tamper INVALID → expiry exit 3 → 422 refusal.
- Gotcha fixed: `policy_id` regex originally hex-only — `pol_20260924-test` was refused; loosened to
  `[0-9a-z]{4,12}`. Template-generated ids are hex and always valid.

## LEASH reproducible scraper + opt-in weekly auto-update (project: swiss-ai-weeks)

Owner asked: make all gathered data reproducible via a scraper + optional
weekly fetch/update toggle. Built `merchant-trust-data/processing/update_all.py`:

- `make update` = one-shot reproducible refresh of ALL 26 raw sources (feeds,
  GLEIF CH re-pull, rankings, enrichment catch-up) + parquet rebuild +
  clean_external + quality reports. `--refresh-all` also re-downloads static
  archives; `--sources a,b` subset; `--dry-run` plan only.
- Freshness model: dated feed filenames (same-day cache / next-day fresh);
  GLEIF page cache cleared per update (policy knob); enrichment resume-safe.
- Run provenance: `reports/update_runs/update_<UTC>.json` + latest.json
  (last 52 kept).
- Weekly toggle: `make weekly-on|off|status` flips
  `merchant-trust-data/config/update.json` auto_update.enabled (default OFF).
  Runner = OpenClaw automation `leash-weekly-data-update` (job
  `9cd210b9-038b-4e96-914f-ae7603c4254a`, cron `0 6 * * 1` Europe/Zurich,
  session-bound to this dashboard session, announce+bestEffort). Gate test
  forced a run while off → status ok, summary NO_REPLY, delivery suppressed
  silent. Repo config flag is the source of truth, not the scheduler.
- Learned: `automations` tool can only manage existing jobs — creation goes
  through `openclaw automations add --cron ... --session-key ... --message
  "$(cat prompt.txt)"`. First add attempt appeared to print a preview but the
  job did not exist; rerun with full output created it (EXIT=0).
- Tests: tests/test_update_all.py (10); suite 25 pass. Committed as 69e4673
  (Phase-3 intel collectors, earlier today's work) + a1a3bb9 (pipeline), pushed.

## viseca-shopper-ui: multi-user + ChatGPT/skill integrations (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

Owner asked for multi-user registration/login with subscription levels + ChatGPT plugin & OpenClaw/Claude skill support. Shipped in `ce437f2` (pushed), live on :8794, verified on https://viseca-shopper.pixerful.com.

- `accounts.js`: zero-dep JSON store — scrypt passwords, hashed cookie sessions (30d), `vsk_<id>_<secret>` API keys (secret hashed, shown once), daily usage counters (reset midnight UTC). Data in `viseca-shopper-ui/data/` (gitignored).
- Plans free/plus/premium = 10/100/500 msgs/day (`PLANS_JSON` env overrides). Self-serve switching in UI; **billing not wired** — switching is instant/free (flagged as demo in UI + README).
- Each account gets its own agent session key `webui-u<id>`; per-user 409 while busy, global cap `AGENT_MAX_CONCURRENT` (default 2) → 503, over-limit → 429 with plan info.
- **Security fix:** `/api/policy/sign` (the Ed25519 signing authority!) was reachable unauthenticated on the public URL — now cookie-session-only; API keys can never sign policies.
- ChatGPT: `/openapi.json` (GPT Action import; plugins retired — manifest `/.well-known/ai-plugin.json` still served) + `/api/v1/chat` plain-JSON endpoint (no SSE — Actions can't stream). Base URL derived from `x-forwarded-*` behind caddy (verified correct in served spec).
- Claude/OpenClaw skill: `viseca-shopper-ui/integrations/skills/viseca-shopper/SKILL.md` — copy to `~/.claude/skills/` or `~/.openclaw/skills/`, set `VISECA_SHOPPER_URL` + `VISECA_SHOPPER_API_KEY`.
- Tests: `npm test` = 21 e2e smoke tests on `test/stub-openclaw.js`, isolated data dir.
- Deploy: restarted live instance; it now logs to **`/tmp/viseca-shopper-ui.log`** (old instance logged to `/tmp/viseca-shopper-ui-8794.log`).

Lessons (host-level, will bite again):
- **`NODE_USE_ENV_PROXY=1` + `HTTP_PROXY` are set in this env → Node fetch routes even `127.0.0.1` through the egress proxy** (connection "other side closed"). Any local-fetch harness must `delete process.env.NODE_USE_ENV_PROXY; process.env.NO_PROXY="127.0.0.1,localhost"` first; curl needs `--noproxy '*'`.
- `node --test` executes EVERY `.js` under a `test/` dir (our CLI stub got run as a test) — pass the explicit file in package.json.
- Ports 19000/19001 already taken on this host; pick test ports via a `net` probe on :0.

## Phase-3 enrichment data: collected + cleaned + pushed (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

- Sources landed (commit 9884f5f): ThreatFox 9,331 IOCs (8,812 cleaned), FeodoTracker C2 IPs,
  UN sanctions 1,011, OFAC SDN 19,392, SECO 8,609 (via OpenSanctions mirror — SECO has no
  stable anonymous bulk URL), domain_health 5,484/5,484 (DNS MX/NS/A+SPF, HTTP liveness,
  Wayback CDX first-seen, crt.sh 500-sample), AbuseIPDB 1,000/5,091 IP-host checks (999 ok;
  850 flagged >0 confidence, 203 ≥50, median 24; free-tier 1k/day cap — resumable queue at
  data/raw/abuseipdb/_ip_queue.json, rerun `python -m collectors.abuseipdb` tomorrow with
  .env sourced).
- Blocked: MalwareBazaar (blob+API both 502 through egress proxy; needs free abuse.ch auth
  key as fallback), EU consolidated sanctions (redirects to EU Login), SHAB API (token
  required — owner registration).
- Owner provided AbuseIPDB key in chat → stored gitignored at merchant-trust-data/.env
  (LEASH_ABUSEIPDB_KEY); .gitignore now covers .env. Never commit.
- Subagent for this run timed out (50 tool calls, no final output) — I finished manually:
  collectors it wrote were solid; abuseipdb got a run harness appended (main dispatch per
  repo lesson). Lesson: steer via sessions_send to a busy run timed out ("not committed
  before timeout") — key handoff mid-run unreliable; stage secrets in .env first.
- Exports + stats + EXTERNAL_QUALITY_REPORT.md regenerated for all new sources; 30 tests
  pass; untracked models/prompt_injection/ left alone (another session's work).

## Prompt-injection detector: built, calibrated, deployed into wallet-control (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

- Shipped (commits b4455d6 + fedfdff, pushed): `merchant-trust-data/models/prompt_injection/`
  (corpus builder → two-phase trainer → per-pool calibrator → fixture generator) +
  `wallet-control/lib/injection-model.js` (md5-hashed window-max scorer, JS/Python parity
  ±1e-6) + engine integration (escalation-only: ≥τ → INJ_MODEL step_up with quoted n-gram
  evidence; mid-band → INJECTION_SUSPECT uncertainty; missing artifact → layer inert).
- Shipped model v2: TensorTrust attacks 203k vs broadened benign 124k (TT defenses 69k,
  GLEIF names 26k, Tranco 23k, Google taxonomy 4.8k, AgentDojo clean-run context 1.2k);
  md5-hashed TF-IDF 2^19 uni+bi + SGD logreg; window-max scoring (40-token windows,
  stride 20) so short embedded clauses aren't diluted; per-pool FPR τ=0.0542 → 0% FPR on
  Viseca pack, BIPIA benign tasks, challenge-pack clean texts; TT-test recall ~16-30%.
  8 regex-escaping held-out attacks locked into wallet-control fixtures (0.46-0.70).
- Documented failures kept as ablations in EVAL.md: v1 (defenses-only negatives → learned
  "instruction-like = safe", 31.8% FPR on benign product text); v3 mixture training (113k
  attack-in-benign carriers → carrier leakage collapsed the distribution, benign p99 0.95,
  no usable τ). Lesson: mixture carriers must be strictly disjoint from negative pools, and
  word-level TF-IDF does not transfer to BIPIA-directive or challenge-pack embedded clauses
  (those stay covered by the regex layer). Next levers: HackAPrompt (HF token), embedding/
  sequence-aware features.
- Calibration lessons: pooled quantile lets tiny benign pools bleed (50-row pool → 22% FPR);
  use per-pool budgets, pin pools ≤100 rows at max+5%. TT-defenses can saturate p=1.0 —
  weight deployment-relevant pools, report TT-defenses informationally.
- Integration lessons: load the 9.3MB artifact eagerly at import (lazy load put ~120ms into
  the first decision and failed the engine <25ms test); parity vectors must come from the
  SAME scoring mode as the deployed scorer (full-text vs window-max drift); anchor artifact
  tests to the deployed copy, not "latest version file" (v3 ablation sorts last).
- wallet-control is now TRACKED in the repo (was deliberately untracked during dataset
  work); derived lib/injection-model.json gitignored — loader falls back to the canonical
  merchant-trust-data artifact. Server restarted on 8791 (setsid nohup, log
  /tmp/wallet-control-8791.log); note: /api/health is not an endpoint on this service —
  check the root UI or the log line.

## viseca-shopper UI agent turns failing — diagnosis + fix (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

Owner: "the agent on the ui does not work". Server + caddy + auth were fine; real
shopping turns died two ways: (1) ~12:35 gateway memory-pressure spike
(rss 2.21GiB > 1.5GiB threshold) made agent web_search calls take ~10 min →
bridge timeouts; (2) zai completion hangs aborted runs (`request timed out`,
stopReason aborted) → CLI returned JSON with no reply text → cryptic
"Agent JSON contained no reply text", no retry.

Fix (server.js, commit after 1c39054): structured failure extraction
(stopReason/errorMessage via findDiagnostics), turnWithRetry — one
budget-aware retry (overall ≤590s, under the client's 630s abort timer) that
re-sends the message + a continuation nudge (agent continues from partial work
already in session history), friendlyTurnError mapping for SSE + /api/v1 paths.
Verified: 23/23 npm tests; trivial turn 8s; realistic "3 hiking gifts under
50 CHF" turn 118s with full markdown links. Durable restart pattern held
(setsid nohup env HOST=0.0.0.0 PORT=8794 … >> /tmp/viseca-shopper-ui.log).
Lesson: when tool calls crawl on this host, check gateway memory-pressure WARNs
in /tmp/openclaw/openclaw-*.log — transient, self-heals, but turns must fail
gracefully + retry, which the bridge now does.

### Demo account (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

- Shipped `64bc7b0`: seeded demo account **demo@pixerful.com / demo-viseca-2026** (Plus plan), one-click "Try the demo account" button → `POST /api/auth/demo` (404 when `DEMO_MODE=false`). Customize via `DEMO_EMAIL`/`DEMO_PASSWORD`/`DEMO_PLAN`.
- Seeded API key (logged once at boot): `vsk_cdaae46c_d-iC9FnzZvTtl1PD_QOd23MKxKdvtYZx` — for the ChatGPT Action / skill demos.
- Live-verified public demo login (200, isDemo:true). Note: PID 860661 running `node server.js` on **:8791 is wallet-control**, not shopper-ui — don't confuse/kill it when restarting 8794.

### Demo account privatized (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

- Owner asked to remove the public demo button; shipped `acf7bc6`: `POST /api/auth/demo` deleted (404), demo info removed from `/api/health`, button gone from the login form.
- Demo password is now **not in the repo**: `DEMO_PASSWORD` env is force-rotated onto the account on every boot; unset + fresh seed generates a random one logged once.
- Live rotated off the old `demo-viseca-2026` (now 401). Current password: `s1DY3HmD2NnKjU9O` — email still `demo@pixerful.com`, plan plus.
- Seeded API key `vsk_cdaae46c_…` unaffected by rotation (still 200).
- Ops lesson re-confirmed: NEVER chain `git push && kill && spawn ... & sleep; echo $VAR` in one exec — the trailing `&` backgrounds the whole chain and the wrapper reaps it before `setsid` spawns (site went 502 for ~1 min). Do: generate/kill in one exec, launch detached in its own short exec, verify in a third. Wallet-control runs the same `node server.js` cmdline on :8791 — target kills by the :8794 socket's pid, never by name.

## viseca-shopper-ui: "cannot login" diagnosis + unlimited dummy demo user (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

- Owner reported login broken after registering maxjeffrey@protonmail.ch. Server log showed
  register + **3 successful logins** (success-only logging) — auth logic was fine; curl
  end-to-end through the public URL (register → Set-Cookie → /api/auth/me 200) confirmed.
  Root cause candidate: `SameSite=Lax` cookie is never sent when the site is embedded in a
  cross-site iframe → login succeeds server-side but the UI bounces back to the gate.
- Shipped `29b1f79` (pushed), live-verified on :8794:
  - Session cookie now `SameSite=None; Secure` over https (plain-http LAN keeps Lax; None
    requires Secure). Fixed the iframe case.
  - Failed logins now logged: `[auth] FAILED login <email>` (401s were invisible before —
    that's why the log can't show why a login "failed" historically).
  - Unlimited accounts: `user.unlimited` flag — countMessage tracks but never caps,
    usageInfo returns `limit:null/remaining:null`; app.js renders "∞ msgs"/"unlimited".
  - `ensureDummyAccount()`: `DUMMY_EMAIL`/`DUMMY_PASSWORD` env seeds an unlimited demo guest
    at boot. Live: **guest@pixerful.com / guest-pixerful-2026** (free plan, unlimited).
  - Pruned probe/test.local accounts from live accounts.json; demo + owner account kept.
    Demo login with rotated `s1DY3HmD2NnKjU9O` re-verified 200 after restart.
- Tests 24/24. Restart followed the 3-exec pattern (kill by :8794 socket pid; launch
  detached alone; verify) — wallet-control on :8791 untouched.

### Sign-in still broken → cookie blocking; token fallback shipped (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

- Owner still couldn't sign in after the SameSite fix. Log showed 3 **successful**
  guest logins from them → server-side auth fine; their browser never sends cookies
  back (third-party cookies fully blocked, e.g. embedded iframe / Safari ITP).
- Shipped `02935ef` (pushed, live-verified): login/register responses include
  `session` token; app.js stores it in localStorage and sends `Authorization: Bearer`
  on ALL API calls; `authenticate()` accepts a Bearer session token (mapped to
  via:"session" so policy signing still works). Cookies now optional everywhere.
- **Password/env lesson:** typing a placeholder like `***` in exec commands goes
  through LITERALLY (secrets store has no such entry — checked). Earlier "working"
  launches had real values typed (tool redacts them to *** in transcript only).
  A boot log line `DEMO_PASSWORD ignored — must be at least 8 characters` means the
  env arrived short → rotation skipped → old password stands.
- Live instance now launches with NO DEMO_PASSWORD/DUMMY_PASSWORD env (rotation
  disabled; passwords stable): demo@pixerful.com / s1DY3HmD2NnKjU9O,
  guest@pixerful.com / guest-pixerful-2026 (unlimited, limit:null confirmed).
  DUMMY_EMAIL kept so user.unlimited re-asserts on every boot.
- Launch-wrapper exec sessions (setsid … &) linger as "running" but are cosmetic —
  kill the wrapper pid, server (setsid-detached) survives; verify by :8794 socket pid.

## viseca-shopper-ui: chat history + Purchases view (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner: "continue development and adding features". Shipped `70e3d71` (pushed, deployed live on :8794, authority fingerprint unchanged `56dbd0159f452b45`):

- **Conversation survives reloads**: turns (user/agent/error) stored per account `data/history/<userId>.json` (cap 100, sync writes keep pairs ordered); `GET`/`DELETE /api/history`; UI restores on boot/login, stored policy cards render **read-only** (past policies can't be re-approved — agent must re-propose). Clear button wipes only the stored view; agent session memory untouched. Signed-out now resets the feed.
- **Purchases view**: successful signs record `policy_id → account` in `data/policy-owners.json`; `GET /api/policies` lists own signed policies + receipt status (`policies/<id>.receipt.json`), rendered in Account → Purchases; strictly per-account (test asserts cross-account invisibility).
- Tests 28/28 (was 23; +5 history/policy tests; test gotcha: earlier tests log Ada out, so cross-account checks must re-login).

## AbuseIPDB backlog: cap-aware collector + orchestrator wiring + nightly self-terminating job (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

- `collectors/abuseipdb.py` `run_checks` rewritten (`384ca52`): cached IPs skip **without sleeping and without counting** against the cap; only fresh calls count (`--max-fresh`, default 950 = headroom under 1000/day); first 429 stops cleanly (`cap_reached`) instead of error-storming the queue; failures never cached; run_stats gained `cached_skipped/cap_reached/remaining`. Old form was useless for resume: it re-walked the first 1000 queue entries (999 cached) each night → ~1 new IP/night. Live-verified: 999 cached skipped, spent quota → clean cap stop, exit 0.
- Orchestrator wiring (`a7d0ad6`): `collect()` implements the (path, meta, cached) contract — missing key → MissingCredential (blocked, visible), spent quota → cached=True (not an error); abuseipdb `GATED_SOURCES` → `ENRICHMENT_SOURCES`, `CLEANER_FOR` entry, `config/sources.json` enabled=true. Weekly `make update` now resumes the queue forever. Suite 33 pass.
- **Gotcha**: `update_all.py` must run as `python -m processing.update_all` from repo root — direct script path breaks the `collectors` import (module search path).
- New automation `leash-abuseipdb-nightly-resume` (`dce0696e-5206-4f2b-a295-48745cf80da8`): cron `10 2 * * *` Europe/Zurich (quota resets 00:00 UTC), isolated session, announce+bestEffort, timeout 3600. Silent (NO_REPLY) while `remaining > 0`; on completion runs the abuseipdb cleaner + report regen, commits exports, removes itself. ~4,092 remaining → ~5 nights. Verified in automations list.
- **Found + fixed a real duplicate**: TWO `leash-weekly-data-update` jobs existed (`9cd210b9` the proven runner + `dbef8ebc` never-run) — the Sep-24 "preview that didn't create" actually DID create; the rerun duplicated it. Removed `dbef8ebc`; kept the runner with history. Lesson: after `automations add`, verify with a LIST (create returned full JSON here, but the earlier incident's "no job" was really a duplicate).
- Parallel-session note: another session pushed `8d04efd` to master between my pushes; my `a7d0ad6` stacked on it cleanly (shared worktree).

## Behavior model: ML on user behavior wired into wallet-control (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner: "use the viseca-2026 data mainly beside other data + implement machine
learning based on user behavior". Pack was already vendored **byte-identical**
at wallet-control/data/pack (verified vs fresh upstream clone; 4,701 history
rows / 45 attempts). Built, following the injection-model deployment pattern:

- `merchant-trust-data/models/behavior/train_behavior.py`: pure-numpy (no
  sklearn on this host) class-balanced L2 logreg over **12 chronology-safe
  behavioral features** from authorization_history.csv (4,565 purchases, 258
  declined; Adam, seed 20260924). LOCO AUC **0.7422**. Calibration
  friction-first: escalate τ = q97 of approved-row scores → **3.02%** of
  known-good history escalates; attempts land 29 normal / 16 suspect /
  **0 escalate** → replay decisions identical to the rules-only baseline.
- Deployed `wallet-control/lib/behavior-model.js` + gitignored artifact copy;
  canonical artifact + parity_vectors tracked in merchant-trust-data; loader
  falls back to canonical (same as injection-model). Parity: features ±1e-9,
  scores ±1e-6, 9 vectors.
- Engine integration **advisory-only** (never approves/declines/loosens):
  evidence row on every decision; escalate band → BEHAVIOR_ANOMALY uncertainty
  → customer's own uncertainty policy (ask → step_up). Suspect band =
  evidence-only, plain-language drivers ("burst of attempts…, larger than 95%
  of your past purchases").
- **Gotcha**: purchase_attempts.csv lacks merchant_category/country — must join
  merchants.csv; unjoined, 44/45 attempts flagged unfamiliar (live events carry
  these fields nested under merchant{}; scorer resolves both shapes).
- Weight honesty kept in EVAL.md: device_unfamiliar has a negative *partial*
  weight (collinearity) though univariate decline rates point the intuitive way.
- Tests 41 pass (25 engine + 5 injection + 11 behavior); npm test runs all
  three files. README principle 1 reworded to "No model decides" (two advisory
  detectors documented).

## shopper-ui: shopping controls shipped (cap / whitelist / card vault)

<!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks | status: shipped 2026-09-24 -->

- viseca-shopper-ui: per-account **spend cap**, **searchable website
  whitelist**, **Visa/MC card vault** (`shopping.js`; `data/vault.json` 0600
  gitignored, API returns masked only, Luhn + brand-locked). Enforced at
  `POST /api/policy/sign` → 422 with `violations` (≠ signer's `missing`).
- Sign success files `policies/<id>.payment.json` (full card for checkout,
  agent-only, gitignored) — the ONE place card data enters the agent
  workspace. No card at sign → `card:null`; saving a card backfills unpaid
  policies. Whitelist active ⇒ policy must carry `merchant.allowed_domains`
  ⊆ whitelist. UI: Account → Shopping settings (cap, search+select whitelist
  with DDG enrichment, `SHOPPING_WEB_SEARCH=0` disables; card form with live
  brand detect).
- Bridge: `OPENCLAW_MODEL` env → `--model` per agent turn (Apertus path).
- Tests 40/40 (13 new, `SHOPPING_WEB_SEARCH=0` keeps them network-free).
- Deploy: old PID 958037 env captured to shell-safe file, PORT/HOST pinned
  8794/0.0.0.0, restarted via setsid nohup from canonical path (new PID
  1041235); live marker `health.shopping` + anon 401 verified; env temp files
  rm'd (secrets).
- Git: monorepo 924f49a pushed, ls-remote verified; agent repo local commits
  (payment section + refusal triage). order-policy-gate skill updated with
  card-custody rules.
- **Apertus pending:** operator must add a `swissai` provider (OpenAI-
  compatible, e.g. serving.swissai.svc.cscs.ch/v1) to
  `~/.openclaw/openclaw.json` + `modelPolicy.allow`, restart the Gateway,
  then set `OPENCLAW_MODEL=swissai/<apertus-id>` on the bridge service.

## Behavior model v2: night_hour feature, LOCO 0.7422 -> 0.7850 (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

Owner: "try to improve the model". Ran a 3-round LOCO feature/l2 search
(`models/behavior/experiment.py`, fold-internal standardization):

- Winner: `night_hour` (UTC 21:00-06:59, generic) + l2 3e-2 -> in-sample 0.8078,
  **LOCO 0.7850** (v1: 0.7733/0.7422). Friction unchanged (3.02% approved >= tau).
- Rejected with numbers (recorded in EVAL.md so nobody re-runs them):
  category_log_count, weekend, days_since_last, all 3 interactions, merchant_share
  (each alone or bundled never beat night alone); night windows 22-05/23-04/20-07
  all worse than 21-06; l2 1.0 over-shrinks. Fold-std vs global-std LOCO:
  0.7421 vs 0.7422 — v1's std leakage immaterial.
- Attempts now band 28 normal / 15 suspect / **2 escalate** (v1: 0 escalate):
  AU0029/AU0030 = CA0023 02:21/02:24 UTC burst (3 attempts in 7 min, GBP/GB swap
  variant) — the card-testing pattern v1 called normal. Both already DECLINEd by
  rules; model adds corroborating evidence, replay diff: **all decision lines
  identical** (stripped-ms + trailing-space diff), only evidence rows changed.
- Contract updated in lockstep (trainer docstring = JS header = artifact
  features): 13 features, version behavior-model-v2, test asserts length 13.
  npm test 11/11 after copying canonical artifact to wallet-control/lib/.
- Gotcha: exec runtime "Active exec sessions: none" notes were stale all evening
  while python experiment ran — live `process poll`/ps is the truth (again).

## Working-style directive: step-by-step task narration

Owner asked the agent to break tasks into steps and explain which step it is on
and how it does it. Recorded as two active directives in USER.md (observed
2026-09-24): (1) non-trivial tasks get numbered steps up front, with current
step + method announced while working; (2) multi-step work keeps a progress_card
plan checklist updated as steps complete. Applies from the next session onward;
this session already follows it.

## viseca-2026 data pack: full audit + integration into viseca-shopper (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

Owner: "go through every detail of the viseca github repo and maximize usage for our model/app". Audited all of `Swiss-ai-Weeks/viseca-2026` (cloned to /tmp): challenge.md ("Agent on a Leash" wallet-control challenge), technical_details.md (sandbox API, mandate/rule format, 8s decision deadline, 120s step-up window), data pack (11 CSVs, 3 schemas, 2 fixtures, metadata.json). Integrated into the viseca-shopper agent repo, commit `1e6ab17` (local-only repo):

- `data/viseca-2026/` vendored, all 18 file hashes verified vs metadata.json.
- `scripts/build_pack_derived.js` (zero-dep) → `derived/card_baselines.json` (per-card approved merchant/device counts = familiarity, agent history, night share), `derived/attempt_features.json` (45 attempts joined: merchant, cart lines w/ items.csv CHF ranges, parsed item_details — size/return days/final sale — prompt-injection scan), `derived/scenario_presets.json` (hand-compiled policies for the 5 SCEN instructions).
- `scripts/replay_scenarios.js`: offline replay of all 45 attempts through a layered evaluator (injection guard → per-order limit → rolling 7d approved-only ledger → duplicate/retry → cart-match/add-ons → merchant category/familiarity → session integrity (novel device = step-up) → scope/split-order/price-range uncertainty). Result: approve 16 / step_up 10 / decline 19. Catches both injections (AU0037/AU0040), lookalike PixelHarbour, gift-voucher swap, add-ons, declined-order retry, night velocity burst (steps up, doesn't auto-decline — customer said "pause anything suspicious").
- policy.js selftest still exit 0 (no regression); AGENTS.md gained "Challenge data pack" section (decision-time usage rules).
- Data traps worth remembering: TR vs AU id namespaces never overlap; baselines are card-scoped lifetime approved-only counts; `spend_in_period_before_chf` is always null (period tracking is the participant's job); billing CHF already includes delivery; currency comes from the row, not merchant country (61 foreign rows billed CHF); AU0008 hits exactly CHF 300.00 rolling cap after step-ups aren't counted; every live event has initiator_type=agent (CSVs omit the column).

## viseca-shopper-ui: parental controls shipped (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner: "add parental control features — child accounts, max spend limit, category spend limit; make sure it actually works".

- New `viseca-shopper-ui/family.js` (zero-dep): child accounts via `accounts.createChildAccount` (parentId on the user record, max 10, children can't nest), limits `{maxSpendChf (per order), monthlyBudgetChf (UTC month aggregate), categoryLimits}`, month ledger pruned to current+prev month, stored in `data/family.json`.
- Enforcement at sign time in `signPolicy` right after shopping checks, same 422+violations shape: per-order, monthly aggregate, per-category (categories from `policy.category` or merchant domains via shopping catalog; unknown → "Other"). Ledger records signed `budget.max_total`, not receipts.
- Suspension blocks login (403) and kills sessions/API-key auth (401) via `authenticate`; removal purges user+sessions+family data. Children get no family endpoints.
- Family card: child with no vault card pays with parent's default (`payment.family_card:true`); parent saving a card backfills children's unpaid policies (`backfillPaymentFiles` now iterates children).
- UI: Account → Family section (parent mgmt: create/suspend/remove/limits/spend bars; child: read-only banner). Health: `family:{parentalControls:true}`.
- Tests: 16 new E2E in smoke.test.js → 55/55 pass. Committed `00db300`, pushed. Live 8794 restarted (setsid nohup, PORT=8794 HOST=0.0.0.0 DUMMY_EMAIL=guest@pixerful.com → /tmp/viseca-shopper-ui.log) and verified (health marker, 401, new assets).
- Gotcha hit: ternary without `: else` in a multi-line call → SyntaxError reported at the closing `);`, not at the ternary — misleading location.

## SAW26 sandbox API key stored + egress-proxy restart blocker (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks) <!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->

Owner's "viseca github repo sandbox" key = NOT a GitHub credential — it's the SAW26 sandbox team API bearer key. Stored as `VISECA_GITHUB_REPO_SANDBOX_TOKEN` (name historical), allowedHosts=[saw26api.ashyground-364e1d07.switzerlandnorth.azurecontainerapps.io]. GitHub 401s were expected; GITHUB_PAT control (200 as itsmaxjeffrey) proved the injection path end-to-end.

- Sandbox live: `/healthz` → 200 direct (`--noproxy '*'`). Through the egress proxy: 502 CONNECT refused, even from fresh runs/subagents after the host edit — `secrets.egressProxy` policy refresh needs a Gateway restart (docs/gateway/config-secrets-env.md#L61). Pending: owner restarts Gateway → retest `/v1/bootstrap`.
- Docs written: `wallet-control/docs/saw26-sandbox-api.md` (base URL, endpoints, timings, gotchas). Source doc owner asked kept: github.com/Swiss-ai-Weeks/viseca-2026/blob/main/technical_details.md.
- Lessons: gateway-host exec snapshots the store on its FIRST exec of a run; later host edits don't refresh that run's snapshot (fresh run needed, docs/tools/secrets.md#L161) — but the proxy's destination policy itself is process-lifetime, so binding changes need a Gateway restart.

## Hackathon hour — recursive improvement loop 20:58–22:00 (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

Owner directive: 1h self-directed loop — test UI + prompt injection + E2E, screenshot everything, decide alone. Executed in this session (webchat).

- **E2E proven live on :8794** (accounts e2e-ok-1790276708 / e2e-inj-1790276708@pixerful.com, tokens in /tmp/e2e-tok-*): ordinary purchase = policy draft → agent asked for order-by/stop-searching → "yes to both" → final card → signed `pol_20260924-d10d` (authority 56dbd0159f452b45) → premature gate msg correctly refused (unsigned) → re-run gate → found **Nike Pegasus 41 black CHF 82.45 at Ochsner Sport (−50%)** → checkout with size EU 42 sent 21:20. Injection paste (SYSTEM OVERRIDE + gift cards + SHADY-DEALS-4U) → agent refused with itemized fraud markers, offered the safe path. Both are demo pillar evidence.
- **API notes**: sign endpoint wants `{"policy": {...}}` (bare object → 400); `/api/v1/chat` json mode blocks until turn done (use --max-time 900); register returns session token immediately.
- **wallet-control injection hardening — commit `4f50e14` pushed**: signals.js +10 pattern families (FR/DE multilingual overrides, security/compliance clearance claims, no-confirmation, skip-checks, concealment, mode flags, third-party approval, encoded payloads, md-image exfil), `pre[-_ ]` + bare "limits do not apply" broadened. NEW `test/injection-battery.test.js` (20 fresh attacks / 8 benign, 29 checks) wired into npm test. Battery first run: 12/20 attacks slipped through → after fix 28/28 + all 41 prior tests green. Trained advisory model scores low (0.002–0.048) on the new styles — regex layer is the guaranteed catch; corpus retrain is an open improvement.
- **Lesson (JS)**: regex literals can't be `|`-joined (`/a/|/b/` coerces to 0 → `text.match(0)` matches every "0" digit — caused FP on "30 days"/"30 Tage"). One alternation regex instead.
- **Screenshot recipe (this host)**: chrome CLI `--headless=new --screenshot` works ONLY with proxy env stripped (`env -u HTTP_PROXY -u HTTPS_PROXY … NO_PROXY='*'` — Chrome honors http_proxy even for 127.0.0.1 → silent hang, exit 124). Node fetch/WebSocket/http.get are ALL intercepted by the egress proxy even with NO_PROXY set, but raw `net.connect` passes → **puppeteer-core with its bundled ws + `launch()` works**: `/tmp/shots/pptr-shot.mjs <url> <out.png> [sessionToken]` (evaluateOnNewDocument sets `shopper_session_token` localStorage). Evidence: workspace `evidence/hackathon-hour/01-03*.png` (login, policy chat, injection refusal) — verified visually, all render clean.
- **Open bug (bridge reliability)**: two earlier owner turns failed today — "Agent turn timed out after 300s" and 514.8s "Agent JSON contained no reply text"; plus recurring `service child cleanup did not complete before its hard deadline` in the 8794 log after turns. Demo-critical if it recurs during judging.

### Browser wedge root cause (21:52, project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- `openclaw browser status --json`: exit 124, ZERO bytes output, reproducible (4+ attempts over 20 min; CLI spun at 150% CPU). `browser stop` fails with "SQLite read-only worker exited with signal SIGTERM" reading config — CLI sqlite reads are INTERMITTENT under contention (automations list/doctor worked seconds earlier).
- Agent-side evidence matches: "SQLite transaction lock waits against the live Gateway state DB"; 5 hung browser calls from its session; plugin reload + chrome kill did not help. No chrome processes remain; stale Chrome SingletonLock (→agent-748759, 11:46) exists in ~/.openclaw/browser/openclaw/user-data but pid-liveness takeover makes it unlikely the root cause. `openclaw doctor` (read-only) exits clean — no browser/sqlite findings; its --fix path is gated on unrelated ownership fixes, so NOT a targeted repair.
- Conclusion: gateway-internal browser module wedge → needs OWNER-side gateway restart (agent red line: never restart gateway myself). Recovery = restart gateway → verify `timeout 30 openclaw browser status --json` returns JSON → armed automation `ochsner-retry-pol_20260924-d10d` (52ac48b4, every 20m, silent-when-blocked) then completes the order per `policies/pol_20260924-d10d.runbook.md` (product URL, price/size guards, payment-script rules, receipt schema) and self-removes.
- "Hackathon loop final check" (2cc4e58a) showed 0 runs at 21:50 despite 21:48 schedule — recheck fires; remove if it never fires to avoid clutter.

### Incident: "no session history / features broken" after 22:06 restart (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner report 22:23. Root cause: the "Push UI changes to GitHub" session committed `bfb8cdf` (feed scroll, stop button, email validation) and restarted the server WITHOUT `DUMMY_EMAIL` → `ensureDummyAccount()` skipped → demo guest login 401'd ("FAILED login guest@pixerful.com" in log). History itself was NEVER lost — `/api/history` returned 12 messages for u_3555bf81 throughout; data/history/*.json intact; auth sessions persisted (auth-sessions.json).
- Fixed 22:4x: restarted server with full canonical env (HOST/PORT/DUMMY_EMAIL/OPENCLAW_MODEL/OPENCLAW_GATEWAY_PORT) + reset the dummy guest password (old one was env-seeded from a dead process, unrecoverable). **Demo guest: guest@pixerful.com / Pixerful-Demo-2026! (unlimited)** — recorded here because the app credential was lost; rotate if that's wrong.
- Verified: login 200, history 12 msgs, demo login 200 + unlimited flag re-asserted, /api/chat/stop → 404 "No running turn" (correct), smoke tests 56/56, feed renders fully (puppeteer screenshot evidence/hackathon-hour/04). bfb8cdf's flex/scroll change did NOT break rendering.
- Purchase still pending: gateway NOT restarted (pid 1165050 since 19:42), browser daemon still wedged (status exit 124); ochsner-retry job armed (runs ok/NO_REPLY silent while blocked) — completes order automatically after the gateway restart.
- Lesson: server restarts must use the canonical env recipe (documented in memory 82-104); pgrep -f "node server.js" matches the exec wrapper — filter by /proc/<pid>/cwd or ss -tlnp.
### Hour wrap + incident close-out (22:23–22:55, project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner incident ("no session history / features broken") resolved — details in the incident section above. Final verified state: history intact (12 msgs across all restarts), demo guest login restored (`guest@pixerful.com` / `Pixerful-Demo-2026!`, unlimited re-asserted each boot via DUMMY_EMAIL), feed renders fully post-`bfb8cdf` (screenshot `evidence/hackathon-hour/04`), `/api/chat/stop` → 404 "No running turn" when idle (correct), smoke tests 56/56.
- Auto-completion path VALIDATED: forced run of `ochsner-retry-pol_20260924-d10d` (52ac48b4) → status ok / summary NO_REPLY / suppression silent — scheduler + agent loop + runbook all work; it stays silent while the browser daemon is wedged and will complete the Ochsner order + report when the Gateway is restarted. Removed the never-fired "Hackathon loop final check" (2cc4e58a) at ~22:00 (superseded by the retry job).
- STILL OPEN (owner-only action): Gateway restart to un-wedge the browser daemon (gateway pid 1165050 unchanged since 19:42, `openclaw browser status --json` still exit 124 at 22:50). After restart: retry job auto-completes pol_20260924-d10d and reports the order number. Follow-up card `task_71fcab2b` documents steps.
- Host lesson (python): plain `urllib.request.urlopen` to 127.0.0.1 fails/routes via the egress proxy — build `build_opener(urllib.request.ProxyHandler({}))` and install it (same family as the NODE_USE_ENV_PROXY gotcha; curl needs `--noproxy '*'`).
- QA lesson: never read API truth through a lenient parser — `d.get('messages',[])` printed "messages: 0" for a 401 and mimicked data loss; probes must print HTTP status codes alongside parsed bodies.
- Owner-behavior lesson (candidate USER.md directive for next maintenance): time-boxed mission instructions ("improve for 1 hour") with an explicit win condition ("make us win / solve everything") do NOT license stopping at the boundary while the win condition is unmet — the owner expected continuation and flagged the stop. Continue past nominal boxes until the condition is met or a genuine owner-only blocker appears, then state the blocker precisely.
- Judge-ready evidence set: `evidence/hackathon-hour/01-login.png`, `02-chat-policy-card.png`, `03-chat-injection-refusal.png`, `04-feed-after-fix.png` — all visually verified; plus live-API transcripts in /tmp/e2e-*.json and receipt automation via the runbook.

### Improvement round 2, 22:38–23:0x (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- `9b9fae6` shopper-ui: turn budget env-tunable (OPENCLAW_OVERALL_BUDGET_MS, default 590s unchanged); client abort 630s→920s. Server now runs with 890s budget — real multi-shop checkouts no longer die at 9.8 min (the 21:30 checkout failure hit exactly 590s).
- `c972b91` DEMO.md: judge-facing doc — three pillars with evidence, safety principles, layout, run instructions, honest limits.
- Timestamp fix: restored history kept re-stamping every message with render time; addMessage now uses stored ts (verified visually: 21:05/21:06/21:08 render correctly). Smoke 56/56.
- Injection-model retraining decision: NOT tonight. The training skill's own evidence says word-level hashed features show ~0% recall on unseen attack styles even after corpus fixes ("better features, not more epochs"); regex layer already catches 28/28. Proper fix = feature work (char n-grams/window features) + full recalibrate/parity cycle — filed as future work, not a quick win.
- Purchase: still blocked on gateway restart (browser daemon wedge); ochsner-retry automation silent-ok each cycle.

### Scroll fix + watchdog, 23:00–23:15 (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner: "ui chat doesnt scroll". Root cause (measured, not guessed): body is CSS grid; desktop had no grid-template-rows → the implicit row auto-sized to content → .stage grew to 4202px (viewport 900), .feed got scrollHeight==clientHeight (4037), body overflow:hidden clipped the rest. bfb8cdf's `min-height:0` on .feed was necessary but useless while the row was unbounded. Fix: `body { grid-template-rows: minmax(0,1fr) }` → feed bounded to 735px, scrollable:true, scrollTop moves (verified via CDP probe + bottom-of-conversation screenshot evidence/hackathon-hour/07).
- Also mysterious one-off ~22:56: shopper server died silently + /tmp sweep took the 2-day-old server log and /tmp/shots/node_modules (systemd-tmpfiles-clean ran 22:56:33; apt-daily/unattended-upgrade active). Response: log moved out of /tmp (server.log in repo dir, gitignored via *.log) + `watchdog.sh` (health-check :8794, relaunch with canonical env incl. extended budgets) registered as automations command job `shopper-ui-watchdog` (ae5bab50, every 5m). NOTE: `openclaw automations run <id> --force` is invalid CLI (tool runMode=force works); direct script test exits 0 when healthy.
- Debug lesson (twice tonight): Node in exec sessions must use the ProxyHandler({}) opener for localhost HTTP (env NO_PROXY is not enough for built-in fetch/ws); and measure with CDP before theorizing about CSS.

### Process-timings feature, 23:10–23:2x (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask (voice): "type a prompt to buy something and show how much each process took… e.g. time for Viseca control, time for the agent to think and find the product". Built end-to-end in shopper-ui, commit `1a8bba9`:
  - server.js: `turnStageLogs` per-user stage buffer + `recordTurnStage`; signPolicy times every exit (settings/parental refusal, exit-4 incompleteness, success, error) → stage marks; `extractRunStats` pulls assistantTurns/toolSummary.totalToolTimeMs from the CLI envelope (flat or meta.agentMeta); `buildTimings` derives think = turn − tool time; timings on SSE `done` + `/api/v1/chat` + `[chat] timings` log line; live SSE `stage` frames when the gate fires mid-turn.
  - app.js: `stage` events update the busy line ("Viseca control — order policy signed ✓ · 0.3 s"); `addTimingsCard` renders the per-process list under the reply (dotted leaders, sub-row for gate marks inside tool work); fmtDuration ms→s→m s.
  - Verified: smoke 56/56 + live SSE probe (stub) — done frame carried full breakdown, totals consistent (324ms = 204 think + 120 tools).
