## viseca-shopper-ui cleanup + symlink incident (project: /home/coffee/.openclaw/workspace/swiss-ai-weeks)

<!-- project: /home/coffee/.openclaw/workspace/swiss-ai-weeks -->

- Owner: "trash the ones u do not need" (post-merge leftovers). Trashed via gio:
  old checkout `~/workspace/viseca-shopper-ui`, dead deploy key file
  `~/.ssh/github_deploy_viseca_shopper_ui`; removed its Host block from
  `~/.ssh/config`. Deploy key 164225718 fully retired locally; GitHub-side key
  dies with the repo.
- **Symlink incident**: at Sep 23 20:36 (after my merge) something replaced
  `swiss-ai-weeks/viseca-shopper-ui/` with a symlink → `../viseca-shopper-ui`
  (the old checkout). Trashing the old checkout broke it → git showed all UI
  files as unstaged-deleted + untracked symlink artifact. History never at
  risk (HEAD 511e98d has full subtree). Fix: trashed symlink, `git checkout --
  viseca-shopper-ui` restored real files, status clean. Origin: unknown — not
  any of my commands.
- UI server 8794 restarted from restored monorepo path; /api/health OK. It dies
  on gateway restarts — restart: `cd swiss-ai-weeks/viseca-shopper-ui && PORT=8794
  HOST=127.0.0.1 node server.js` (log /tmp/viseca-shopper-ui-8794.log).
- **GitHub repo `itsmaxjeffrey/viseca-shopper-ui` NOT yet deleted**: stored
  GITHUB_PAT now 401s (Bad credentials on /user — worked yesterday). Awaiting
  fresh token, then `DELETE /repos/itsmaxjeffrey/viseca-shopper-ui` (needs
  delete_repo scope; classic PAT or fine-grained w/ Administration rw).
- FYI: trash also contained BOOTSTRAP/HEARTBEAT/IDENTITY/SOUL/USER .md files I
  did not trash — owner or another session's cleanup; left alone.
