
### Live activity trail feature, 23:52–00:35 (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask: replace the hard-coded one-line "thinking…/browsing…" status with real per-step reporting (what site is being visited, what is being searched), with checkmarks, growing list, nothing lost. Built + shipped, commit `4da9572`, live on :8794:
  - Design: gateway transcript store proved unusable for live polling (agent sessions dir empty, SQLite `session_state_events` = lifecycle only) → **agent self-reporting**: bridge appends a per-turn token + curl instruction to the prompt (`activityNote`); agent POSTs `{"label"}` to `/api/chat/activity` (token→turn scoped, 40-entry cap, 2KB body cap); SSE `activity` frames + final `trail` array on done/`/api/v1/chat`. `ACTIVITY_REPORTING=off` disables.
  - UI: live trail above composer (earlier steps green ✓ + per-step durations, current step pulsing red dot, elapsed/tokens footer); on finish it freezes into a "What the agent did — N steps" card in the feed above the reply; Viseca-control gate events join the same trail. THINKING_WORDS only until the first real step arrives.
  - Bug found by visual probe: `buildTimings` mislabeled activity marks as "Viseca control — order policy signed" (fixed: skip kind=activity). Stub: STUB_STEP_DELAY_MS env + never echoes the activity note. Smoke 56/56. Evidence: `evidence/hackathon-hour/09-activity-trail-live.png` (pulse mid-turn), `10-activity-trail-final.png` (frozen card + clean reply + timings).
- Guest login broke (401): stored hash didn't match the documented `Pixerful-Demo-2026!` (memory note was stale or account was created with another password). Fixed deterministically: offline scrypt salt+hash reset to the documented value + restart; login 200 verified. Canonical env now seeds DUMMY_EMAIL only (existing account's password is never touched by boots — `ensureDummyAccount` only uses DUMMY_PASSWORD at creation).
- Lesson: masked/credential-looking values in commands reach processes literally — never pass a masked placeholder as an env value (my scratch server once crashed: `DEMO_PASSWORD=***` → "Password must be at least 8 characters"). Verify credentials offline against the stored hash instead of endpoint archaeology.

### ChatGPT-style conversation sidebar, 00:46–01:0x (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask: session picker in the left sidebar like OpenClaw/ChatGPT. Shipped, commit `a1c7c22`, live on :8794:
  - Server: per-account conversation store `data/history/<uid>/<sid>.json` ({id,title,created,updated,messages}); legacy single-history file migrates into "Previous chat" (id "main") that KEEPS its old agent key (`webui-u<sid>`) so old context survives; new sessions run on `webui-u<sid>-c<s_id>`. Turn resolution: adopt requested session, else lazily create (unknown ids are never resurrected — fresh session returned; client adopts done.sessionId). Titles auto-set from the first user message (48 chars). Endpoints: GET /api/sessions, POST /api/sessions/delete|rename; /api/history is ?session= scoped.
  - UI: "00 Conversations" rail section (+ NEW CHAT button) above Start Here; items = title + relative time + hover ✕; active highlighted (red border); switching resets feed + loads that session; new chat = lazy (appears in sidebar after first message).
  - Test gotchas: (1) free plan daily=2 in smoke env — my second test user burned bob's quota, 429 → no done frame; use a fresh user per chat-heavy test. (2) Email collisions across tests cascade 409s (my "carol" collided with the shopping test's carol → renamed to sess@example.com). Smoke 57/57. Visual evidence: `evidence/hackathon-hour/11-sessions-sidebar.png` (list + active highlight) and `12-sessions-switched.png` (switch worked, own agent key per session visible in stub reply).

### Session flush — tonight's shipped state (00:59) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Three features shipped and live on :8794 tonight, all smoke-green and committed: process-timings card (`1a8bba9`, see 2026-09-24.md), live activity trail (`4da9572`), ChatGPT-style conversation sidebar (`a1c7c22`). Smoke suite now 57 tests (sessions coverage included).
- Guest demo login VERIFIED working after the offline scrypt reset: `guest@pixerful.com` / `Pixerful-Demo-2026!` → 200 (the password was broken before; entry above documents the fix). Canonical launch env seeds DUMMY_EMAIL only — boots never touch an existing account's password.
- Live deploy pattern that works: `kill $(ss -tlnp | grep 8794 …pid)` → `setsid nohup env HOST=0.0.0.0 PORT=8794 DUMMY_EMAIL=… OPENCLAW_MODEL=zai/glm-5.3 OPENCLAW_GATEWAY_PORT=18789 OPENCLAW_TIMEOUT_MS=900000 OPENCLAW_OVERALL_BUDGET_MS=890000 node server.js >> server.log 2>&1 &`; trailing "service child cleanup" stderr noise is the OLD process dying, not a fault. A leftover idle exec shell (nimble-crest) from the deploy wrapper is harmless — the server is setsid-detached.
- UI evidence set now spans 09–12 (`evidence/hackathon-hour/`): live pulsing activity trail, frozen trail card + timings, sessions sidebar, session switch.

### Activity status v2 — headline names the real current step (02:0x) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask (follow-up to `4da9572`): the top line still rotated hardcoded "thinking…/browsing offers…/comparing prices…" and never said what the agent was actually doing. Diagnosis: the ✓ trail existed but real-agent reports were sparse (only ever proven with the stub), the headline showed only elapsed/tokens, and THINKING_WORDS filled every gap with fake specifics.
- Shipped (UI `8487e1e`, agent repo `8983c99`, live on :8794 as `build: activity-live-2` via `systemctl --user restart viseca-shopper-ui.service`):
  - app.js: headline = last trail entry's label verbatim ("Visiting digitec.ch · 1m12s · 44.3k tok"); THINKING_WORDS rotation deleted, fallback is a single honest "thinking…"; 1 s interval now only re-renders elapsed.
  - server.js `activityNote` rewritten: plan-first label, then mandatory curl (now `--noproxy '*'`) at EVERY step START with real query/domain labels ("Searching the web for <actual query>", "Visiting <domain>", "Comparing prices: X vs Y", "Checking out at <domain>"); note still starts `(System:` — stub-openclaw.js splits on that exact prefix. Activity POSTs now log `[chat] activity user=…: label` (they were silent, which made "0 in server.log" look like proof of failure — it wasn't); `/api/health` gained a `build` marker for deploy verification.
  - viseca-shopper AGENTS.md: standing "Live progress reporting" section — reporting mandatory when the token note is present; never batch, never invent domains.
- Verified: node --check, 57/57 smoke, health marker, live fresh-account SSE turn (start→done 3.6 s, trail [] as expected for trivial), and session history showing the new note reached the agent verbatim.
- Curl-proxy red herring: `HTTP_PROXY` set but 127.0.0.1 GETs return 200 through the egress proxy anyway — `--noproxy` added for hygiene, not the root cause.

### Stop button that actually stops the agent, 02:05–02:50 (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask: stop button for the chat that stops the AI from working. Finding: a stop button existed (`bfb8cdf` — header "stop" label + `/api/chat/stop`) but it NEVER actually stopped the agent: it SIGKILLed the bridge's direct child, which is just the `openclaw` launcher; the real CLI runs as an anchored grandchild in a supervisor-owned process group and kept working (proof: activity frames 4+ min after stop; the interrupted run completed at 560.9s with a full reply that silently landed in session history).
- Failed approaches (all live-tested): v2 "stop" chat message to the session = queues a PHANTOM turn, never aborts CLI-spawned runs (the docs' abort phrases apply to inbound channel messages); `chat.abort {sessionKey}` / `sessions.abort` RPCs = "unauthorized" for runs the caller connection didn't start (vacuous `{ok,runIds:[]}` when idle); process-group kill (`detached:true` + `kill(-pid)`) = misses the anchored grandchild; exitCode-alive backstop = skipped because the launcher's exitCode is already set while the run streams.
- v4 that WORKS (`846e321`): agentTurn injects unique `VISECA_TURN_TOKEN` env into the CLI spawn; `/api/chat/stop` scans `/proc/*/environ` for the token and SIGKILLs every match (reaches the anchored grandchild wherever the supervisor parked it) + 12s group-kill backstop. Also: turnWithRetry skips the continuation retry when stopped (a stop-abort looks exactly like no-reply — without the guard the AI would RESTART after stop); stop button moved from the tiny header label into the composer as a send↔stop morph (`.composer.busy` needs a pointer-events override for it).
- Verified per deploy: node --check, 57/57 smoke, boot marker in journal, live E2E (fresh account, stop at 10s → `killed CLI pids: 211924,211932`, turn FAILED 10.0s logged, SSE terminal "Stopped —" frame at 15.1s, zero post-stop activity). Service deploys via `systemctl --user restart viseca-shopper-ui` (user unit since ~01:44 — the setsid nohup recipe is dead for this app).
- Demo-user leftover: their 02:22 stop ran to completion pre-v4; the full agent reply is in that conversation's history but was never shown in their open chat view.

### wallet-control: Trusted Shops merchant verification feature (03:13–04:0x) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask: wallet-control should automatically + concurrently check whether merchant websites the agent suggests are listed on Trusted Shops (any country domain). Shipped in commit `5433521`; 14 new hermetic tests (55 total), live-verified.
- **Trusted Shops public API (verified live, no key):** `GET https://api.trustedshops.com/rest/public/v2/shops.json?url=<bare-hostname>` → 200 `{response.data.shops:[{tsId,url,name,languageISO2,targetMarketISO3}]}` or 404 `SHOP_URL_NOT_FOUND` (= genuinely not registered — digitec.ch, brack.ch, landi.ch, galaxus are NOT TS members; conrad.de IS, conrad.ch is NOT: registration is per registered domain). `GET .../shops/{tsId}/quality.json` → `qualityIndicators.reviewIndicator` (overallMark 4.79 "EXCELLENT", totalReviewCount 1703, reviewsCountedSince). ONE global registry behind all country sites (.com/.de/.ch/…) — `targetMarketISO3` (CHE/DEU/AUT/EUO…) says which market a registration is certified for; suffix matching means `m-s-v.eu` finds `www.m-s-v.eu` entries. Latency ~150 ms/call via egress proxy. Dead ends: legacy profile URL patterns `bewertung/info_{tsId}.html` + `certificate_{tsId}.html` both 404 now; there is NO public name-search endpoint (only url lookup); the shop-search SPA (shop-search-cdn.trustedshops.de, Next.js) hides its API and server-renders nothing for `?searchWords=`.
- **Implementation:** `lib/trustedshops.js` — `TrustedShopsChecker` class: 8-way concurrency pool, 4 s per-request deadline, 6 h TTL cache, in-flight dedupe, injectable `fetchImpl` for hermetic tests. Agent-facing: `POST /api/trustedshops/check {merchants:[…]}` + `GET ?merchant=`; measured 5 merchants ≈ 305 ms. Automatic path: worker pre-fetches the check for any merchant event carrying a website (1.2 s race cap, never risks the 8 s platform deadline; late results land in cache for later events) and passes `extras.trustedShops` into `evaluate()` (new optional 5th arg, engine 1.2.0). Advisory only by design: listed → evidence line + `TRUSTEDSHOPS_LISTED` positive flag; not-listed → neutral evidence (absence ≠ guilt); failed check → silent. Name-only merchants answered honestly (`no domain supplied`) — never invent domains.
- **Node lesson (bit hard):** `AbortSignal.timeout()` never keeps the event loop alive by design — with a pure-hang fetch, the loop drains and Node exits (unsettled top-level await, exit 13) BEFORE the abort fires. Timeout wrappers must enforce their own ref'ed `setTimeout` via `Promise.race` (keep the signal too, to abort real sockets).
- Test-count gotcha: a listed check makes 2 HTTP calls (lookup + quality) — assert on lookup-count, not total calls. `npm test` order matters: trustedshops suite runs last in package.json.

### trustedshops v2 — country-site sweep, non-member profiles (03:55–04:3x) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner correction: digitec DOES show on trustedshops.ch/shops/?q=digitec.ch — v1's member-registry-only check was wrong. Root cause: TS has (a) a MEMBER registry (REST api.trustedshops.com rest/public/v2 — buyer-protection members only, what `shops.json?url=` returns) and (b) SHOP PROFILES on every country site incl. non-members. digitec/brack are non-members with profiles; brack.ch's profile: 1/5 from 17,419 reviews.
- Country-site search API trick: every trustedshops.* domain server-renders `/shops/?q=<query>` and embeds results as JSON in the page's `__NEXT_DATA__` script (`props.pageProps.shops[]` = {profileType member|non-member, accountName, tsID, shopName, shopUrl, averageRating, reviewCount, profileUrl}). No public JSON search endpoint needed. Domain list (owner-confirmed): ch de at co.uk fr it es nl be pt pl eu — `.com` 404s, `.ie` redirects. Same Next app (buildId Q_QcyYlNTuxF_VS22QOHQ) across domains.
- Gotchas: country searches are FUZZY (q=brack.ch returns Sodapop GmbH!) → exact shopUrl/shopName match only, else false positives. conrad.de's member registry = 25 filiale store-page entries, no exact-host entry, quality.json 404s for them → member ratings rescued from same-tsId country-site hits; prefer rated entries as primary (primary is always member-shop shaped). Non-member tsIds 404 on quality.json (expected).
- v2 shipped (commit after `3f9e5b0`): checkOne = 1 member lookup + 12 country searches merged → {listed, found_on[], shops[] (members), profiles[], primary, search_errors[]}. Concurrency lesson: the cap must be REQUEST-level (a semaphore around every fetch) — each merchant fans out to 13 requests, so a merchant-level pool of 6 meant 72 in-flight. Cold 5-merchant batch ≈ 4.6 s (65 reqs), cached 0 ms; single merchant ~1–2 s. Worker pre-fetch cap raised 1.2s→2.5s. 20 hermetic tests + live smoke green; live on :8790 (pid in /tmp/wallet-control.log).

### trustedshops fake-shop check — all 12 country warning lists (04:12–04:3x) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask: also check the trustedshops.*/fake-shops/ pages (URL differs per site version). Discovery: `/fake-shops/` exists on all 12 domains, HubSpot-based (NO __NEXT_DATA__), carries curated warning cards of the exact form `Fake Identity|Trustmark|Shop <domain> <DD.MM.YYYY>` + "N Personen geschützt". There is NO public per-URL fake-shop checker API (REST guesses 404; fakeshop*.trustedshops.com hosts don't resolve; page JS is only HubSpot CMS chrome).
- Implementation (commit `d594128`): parse LABELED warnings only (regex `/Fake\s+([A-Za-zÀ-ÿ]{2,30})[^A-Za-z0-9.\-]{0,40}((?:[a-z0-9-]{2,60}\.)+[a-z]{2,12})/` over tag-stripped text; separator class must exclude uppercase or it eats the type's first letter → "dentity"). Chrome domains (trustedshops/hubspot/cloudflare/jsdelivr/…) excluded via FAKE_SHOP_EXCLUDE. Lists cached 1 h (own TTL, separate from 6 h merchant cache), fetched once per TTL through the request gate and shared across merchants; subdomains of a flagged domain also flag (scam ops rotate subdomains).
- Engine semantics: `fake_shop.flagged === true` → hard fail `TRUSTEDSHOPS_FAKE_SHOP` (decline), ranked right after TRUSTLIST_HIT — a public scam warning is known-malicious, not "missing evidence". `fakeFlagged` suppresses the listing evidence line so the grid never shows contradictory TS lines. Unreachable lists → `flagged: null` (never a fail).
- Gotcha that bit twice: `#fakeShopData()` RESOLVES with per-site errors (never rejects) → the all-checks-failed gate must test `sitesChecked === 0`, not `.error`, or total outages degrade to `listed:false` instead of `null`.
- Live-verified: 99 warnings parsed across all 12 sites, zero fetch errors; `cix.nl` from the live list self-flags (type "Shop"); digitec clean (listed, not flagged). 95 tests total green; server on :8790 restarted with the build (pid 299205, log /tmp/wallet-control.log).

### Yellow-list merchant dossier feature, 04:2x–05:1x (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask: a domain on NEITHER whitelist nor blacklist ("yellow") gets a full dossier before the customer decides — Zefix (registered? Swiss?), imprint parse + Zefix↔imprint compare, LinkedIn/Instagram presence, company age, payment methods, country vs customer, reviews, exact product URL; customer approves → auto-whitelist. Shipped in wallet-control, commit `4852357`, pushed (11 files, +1375).
- `lib/impressum.js`: faithful importable port of the shopper repo's impressum-check.js parser (CLI script stays the authority). `lib/yellowlist.js`: MerchantDossier — Zefix via **token-free Lindas SPARQL** (`https://lindas.admin.ch/query`, graph `https://lindas.admin.ch/foj/zefix`, company IRIs `register.ld.admin.ch/zefix/company/<id>`; `schema:legalName` exact → CONTAINS fallback; UID from identifier IRIs `/UID/CHE…`; seat address incl. street/PLZ/city/region), imprint compare (jaroWinkler name + city/postal/street → strong/partial/mismatch; UID match decisive), socials/payments homepage scan, TS rating via shared TrustedShopsChecker, product-page schema.org aggregateRating; 6 h TTL + inflight dedup + semaphore.
- KEY FINDING: the open Zefix extract has **no registration dates** — company age needs the free REST token (`LEASH_ZEFIX_TOKEN` = "email:token" basic auth, still pending from zefix@bj.admin.ch); public zefix.ch is an Angular SPA, not scrapeable. Dossier reports age honestly as unknown with the token hint.
- Engine: `MERCHANT_UNREVIEWED` uncertainty (→ step_up under ask policy) for a merchant domain that is not trusted, not familiar, not blacklisted; customer-trusted domains give `MERCHANT_TRUSTED` positive evidence AND satisfy `merchant.familiar_to_customer`; blacklist (TRUSTLIST_HIT / TRUSTEDSHOPS_FAKE_SHOP) beats yellow. Store: persisted trusted_domains (subdomain coverage; URL-tolerant). Worker: `resolveStepUp(…, {whitelist:true})` persists the domain. Server: `GET/POST /api/merchant/dossier`; pending step-ups carry `merchant_site`. UI: dossier card + "🤝 Trust merchant & approve" / "Approve once" / "Decline".
- BUG FIXED in existing engine (exposed by new test): `trustLookup` compared the normalized merchant name against a NON-normalized malicious-domain base → hyphenated threat domains could never match by name. Base now normalizeName'd too.
- Live-verified: denner.ch → Zefix found (Denner AG, CHE-105.904.292, seat Zürich), compare **strong**, LinkedIn+Instagram found, CH=CH, TS profile; rebuy.com + conrad.de → imprint bot-walled → honest unknowns, DE≠CH via TLD (weak-signal labeled). First conrad.de run rendered `[object Object]/5`: TS `primary.rating` is an OBJECT `{overallMark, totalReviewCount}` — flattened by new `tsShopRating()` helper + regression test.
- Tests: 22 new (`test/yellowlist.test.js`, custom harness like engine.test.js — run `node test/yellowlist.test.js`; added to the npm test chain). Full suite 117 green. Ops gotcha: a detached `cmd &` server dies silently when its exec session is reaped — start scratch servers with exec `background:true` and kill via `process kill`.
- NOT wired (flagged as follow-up): the shopper-bridge whitelist (`viseca-shopper-ui/shopping.js`, sign-time gate) is a separate store — a wallet "Trust & approve" currently adds to wallet-control's trusted list only.

### Pre-compaction flush addendum (04:54) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Follow-up task card filed for the yellow-list bridge-whitelist sync (see "Yellow-list merchant dossier feature" above): `task_9cdcb570-e40c-4fc6-94c4-f0d52f9ae08a` — sync wallet trusted-merchants (wallet-control `trusted_domains`) into the shopper-bridge sign-time whitelist (`viseca-shopper-ui/shopping.js`), preferred approach localhost-only internal endpoint + shared-secret env, best-effort from `resolveStepUp({whitelist:true})`. Everything else from this session was already recorded in the yellow-list entry above.

### Trusted-merchant → bridge whitelist sync SHIPPED (05:0x) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- The follow-up flagged above is DONE — commit `a89ff5b` pushed, remote head verified via `git ls-remote`. Option (a) as preferred: bridge gained `POST /api/internal/shopping/whitelist` (in `viseca-shopper-ui/server.js`) with guard order: `SHOPPING_SYNC_TOKEN` configured (else 404 disabled) → peer is 127.0.0.1/::1/::ffff:127.0.0.1 (else 403) → constant-time bearer compare (401) → `findUserByEmail` (newly exported from accounts.js; 404 unknown) → `shopping.addWhitelist` (201 new / 200 already-covered / 400 junk domain). Task sketch said `lib/shopping.js` — the module is actually at the bridge repo ROOT.
- wallet-control: `Worker.syncBridgeWhitelist(domain)` best-effort POST (4s AbortSignal timeout, catches everything, returns `{skipped}|{ok,status,added,already,error}`); `resolveStepUp({whitelist:true})` awaits it after the approval and records the outcome as a `bridge_sync` field on the `trust` feed entry + `bridge_sync` in the return. Server env: `SHOPPER_BRIDGE_URL` / `SHOPPER_BRIDGE_SYNC_TOKEN` / `SHOPPER_BRIDGE_USER` (bridge account email — wallet-control has no bridge identity, the endpoint resolves the account). Token never logged.
- Tests: 10 new in `wallet-control/test/bridge-sync.test.js` (house harness, stubbed `fetchImpl`: unconfigured/success/already/HTTP-error/non-JSON-body/network-throw/resolveStepUp-integration/best-effort-failure/decline), added to the npm test chain; bridge smoke 57/57; scratch-port curl suite 11/11 incl. non-local-peer 403 (HOST=0.0.0.0 instance + curl via LAN IP) and disabled-endpoint 404.
- PENDING (owner deploy, not done by design): bridge systemd unit needs `SHOPPING_SYNC_TOKEN=<secret>` then `systemctl --user restart viseca-shopper-ui`; wallet-control restart needs `SHOPPER_BRIDGE_URL=http://127.0.0.1:8794` + the SAME token + `SHOPPER_BRIDGE_USER=<email>`. Until then the wallet trust feed shows "shopper-bridge sync failed: Internal sync endpoint is disabled…" — approvals unaffected (best-effort by design).

### Trusted-merchant → bridge sync DEPLOYED live (05:1x–05:4x) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Deploy executed per merchant-trust-sync skill. Secret `openssl rand -hex 32`, never echoed to transcript. **Bridge**: token in 0600 `/home/coffee/.config/viseca-shopper-ui-sync.env`, loaded via new `EnvironmentFile=` line in the unit (backup `viseca-shopper-ui.service.bak-20260925-pre-sync-token`); restart 05:18:25 (NRestarts=0 since). Verify: no-token POST → **401 `{"error":"Bad sync token."}`** (was 404-disabled).
- **wallet-control**: canonical restart env is now MINIMAL 0600 `/home/coffee/.config/wallet-control.env.sh` — `NO_PROXY`/`no_proxy=127.0.0.1,localhost` + `SHOPPER_BRIDGE_URL=http://127.0.0.1:8794` + `SHOPPER_BRIDGE_SYNC_TOKEN=<same>` + `SHOPPER_BRIDGE_USER=a@b.c` + `PORT=8790`/`HOST=127.0.0.1` pins. Relaunched (pid 363685); boot marker `[leash] shopper bridge sync: http://127.0.0.1:8794 (account a@b.c)`.
- **KEY HOST LESSON — egress proxy eats loopback fetch:** exec-launched node inherits `HTTP_PROXY`/`HTTPS_PROXY` + `NODE_USE_ENV_PROXY=1` from the gateway wrapper → undici proxies even `127.0.0.1` POSTs and the egress proxy fails them (`fetch failed`) while `curl --noproxy '*'` on the same URL works. Fix: `NO_PROXY=127.0.0.1,localhost` (Node 24 env-proxy honors it); external TrustedShops/Zefix still ride the proxy. Traps hit: (1) grep pattern `[Pp]roxy` is case-sensitive and missed `HTTP_PROXY`/`NODE_USE_ENV_PROXY` — grep -i names; (2) an `unset` at the TOP of a captured env file is silently re-overridden by the captured `export HTTP_PROXY=` lines below it; (3) the stale 50-var captured env broke external fetches (dead cert/proxy details) — minimal "ambient session env + NO_PROXY + app vars" beat the full capture (combo test loopback 401 ✓ + trustedshops 200 ✓).
- **E2E VERIFIED (05:37):** sim events never carry merchant domains (`sim/events.js` hardcodes the merchant block; pack `merchants.csv` has no URL column) so live-service step-ups can't exercise the sync. Used a scratch wallet (own data dir, per skill scratch-port rule) with a one-line `merchant_url` passthrough + TrailSpark→`https://www.denner.ch/`, aimed at the LIVE bridge with the real token. Trust-approve (`POST /api/stepups/<id>/resolve {decision:'approve',whitelist:true}`) → `bridge_sync {ok:true,status:201,added:'denner.ch'}`; trust feed `"also whitelisted in the shopper bridge"`; live `data/shopping.json` gained `whitelist:["denner.ch"]` under `u_c10294d0` = a@b.c (accounts.json users are keyed by numeric id; the `u_` id is the entry's `id` field). Scratch + temp secrets rm'ed (secret-bearing temps: rm beats trash).
- `SHOPPER_BRIDGE_USER=a@b.c` chosen as the owner's actively-used shopper account (journal activity 04:17); change = one line in the env file + wallet restart. Live wallet demo ledger now holds the E2E mandate `TM_SIM_e5e732a17733` + run `run_c24c8b028b07` with pending TrailSpark cards whose `merchant_site` is null — trusting those safely no-ops the bridge sync (`resolveStepUp` gates on `rec.merchantDomain`).
- Zefix credentials attempt (05:0x–05:1x): owner supplied username+password via the protected secret store (`LEASH_ZEFIX_USERNAME`/`LEASH_ZEFIX_PASSWORD`, egress-locked to www.zefix.ch). **Lesson: gateway-host secret injection provides OPAQUE SENTINELS, not real values** (fingerprint: 95-char non-email "username"; homepage sanity 200, so the pipe was fine) — sentinels only work where the egress proxy can substitute them verbatim; a Basic-auth header built in-process (base64) hides them, so Zefix's 401s were sentinels being rejected, not wrong credentials. Rule of thumb: the protected store serves config SecretRefs / proxy-substituted fields; credentials for **constructed auth headers** must go into the service's own env (never chat). Code shipped anyway (`9e224b7`): `zefixAuthHeader` accepts split pair + trim, combined-token fallback, tests 24/24. Store entries kept pending owner decision.
- Zefix REST verified LIVE with owner credentials (05:4x, commit `7e7bcab` pushed): creds in `~/.config/leash-zefix.env` (0600, `LEASH_ZEFIX_USERNAME`/`LEASH_ZEFIX_PASSWORD`, Basic auth — the collector's `Token` scheme was speculative). API facts: search POST body `name` must be a plain STRING (array → 400 "Failed to read request"); routes are `/api/v1/company/search` + `/api/v1/company/uid|ehraid|chid/{id}` (bare `/company/{id}` 404s); detail by ehraid returns 27 fields — **the Zefix API exposes NO registration/founding date anywhere** (only sogcDate = last SHAB publication, deletionDate). OpenAPI spec: `www.zefix.ch/ZefixPublicREST/v3/api-docs` (the `/api-docs` + `/v2` variants 401).
- Company age solved token-free: local GLEIF-CH raw chunks carry `attributes.entity.creationDate` + `registeredAs` (CHE UID) → `wallet-control/scripts/build-gleif-ages.mjs` → `data/gleif_ch_ages.json` (38,222 records scanned → 20,456 UIDs, 1.5 MB, committed). REST now also adds **register-status enrichment**: ACTIVE vs dissolved + deletionDate; a dissolved company is a hard negative in the dossier summary.
- Live e2e green: denner.ch → status_active true, legal form AG, strong imprint↔registry compare, LinkedIn/IG, CH=CH; Denner not in the GLEIF subset → age honestly unknown ("API does not expose registration dates"). Tests 26 yellowlist / 131 total.
- Tooling lessons: (1) the write-tool sanitizer turns inline `'Authorization': <expr>` object literals into literal `***` (runtime SyntaxError) — build auth via `headers[authKey] = value` with split key names, or splice code via Node anchor scripts in exec heredocs. (2) shell loops reusing one /tmp output file clobber earlier downloads (lost the api-docs to a later 401 body). (3) detached `cmd &` scratch servers die silently when their exec session is reaped — use exec background:true + process kill.

### SAW26 sandbox UNBLOCKED — plaintext key path works (05:5x) (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner placed team key at /home/coffee/apikey.test; direct call (no proxy) `/v1/bootstrap` → **200**. Team33, api 0.1.0, pack saw26-hackaton-api, profile PROFILE_AUTH0101 "Omar Chen" (Winterthur), 10 scenarios (SCEN0101 connection check → SCEN0117 category exclusions), limits 8s decision/120s step-up/25s long-poll.
- Key installed: `wallet-control/.saw26-key` (600, gitignored). Docs updated with working recipe. Egress-proxy 502 on this host remains an openclaw quirk — ALWAYS call sandbox direct with `--noproxy '*'`.
- **Lesson (repeat of yesterday's, cost us ~30 min):** key material in exec command text gets swallowed/redacted to a literal `***` — my first tests authed with the string "***". Pass secrets via file read INSIDE a script (`/tmp/x.sh` written via write tool, reads key at runtime); never inline in command text.

## viseca-shopper mobile UI overhaul (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)

Owner: phone UI bad → rebuilt mobile experience. Live on :8794, no restart needed (server serves public/ fresh per request). Evidence: evidence/mobile-ui/*.png (before-landing, after-landing-clean, after-drawer, after-sheet, after-desktop-2).

- Layout ≤960px: stage = full-viewport chat frame (100dvh, internal feed scroll, composer pinned bottom, safe-area padding); rail → off-canvas drawer (`body.rail-open`, z-90, backdrop body::before z-89) toggled by new `.m-menu` hamburger in stage-head. Drawer script is INLINE in index.html (app.js untouched — 4 other sessions concurrently editing server.js/app.js/accounts.js).
- ≤640px: overlays (auth/account/onboarding) become full-screen sheets with sticky sheet-head; #turnCounter hidden; type scale reduced.
- Touch fixes: all inputs ≥16px (iOS focus-zoom), `touch-action: manipulation`, tap-highlight off, `.conv-del` always visible (no hover), composer-hint copy swapped to "Replies come from the live agent" via `(pointer: coarse)`.
- viewport meta: `viewport-fit=cover, interactive-widget=resizes-content`.
- Gotcha: defining `.m-menu` only inside the media query left it VISIBLE on desktop (button default display) — base `display:none` rule required. Caught in desktop regression shot.
- Deliberately NOT committed: shared dirty tree (isolation/risk-scoring/onboarding/feedback sessions mid-edit). Changes confined to public/styles.css (960-block rewrite + appended MOBILE POLISH section) + public/index.html (4 small edits); other sessions' hunks are appended blocks at EOF, verified non-overlapping via git diff hunks.

### shopper-ui: first-run onboarding wizard shipped, 06:04–06:30 (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
- Owner ask: "user onboarding — something simple and easy but something super good." Shipped commit `321e507`, live on :8794 (health `build: onboarding-1`), smoke 58/58 (+1 onboarding test).
- Server: accounts gain `onboardedAt` (`markOnboarded`, idempotent) + `publicUser.onboarded`; new `POST /api/account/onboarded` (session/key auth, 401 unauth). Flag rides every auth response (register/login/me).
- UI: `#onboardingOverlay` 3-step wizard auto-opens on first sign-in when `!user.onboarded` (guards: not while the auth sheet is open or a turn is busy; once per page load): ① spend cap chips CHF 20/50/100/250 + custom + no-limit → PUT cap; ② 10 trusted-shop chips → whitelist add/remove applied immediately on toggle; ③ recap + 3 personalized starters (budget + picked shops interpolated) that prefill the composer and focus it. ANY exit path (skip/×/Esc/backdrop/Finish/starter) POSTs onboarded — never re-nags; partial choices are already applied, so skipping loses nothing.
- Verified: node --check ×3; live E2E fresh-account register → `onboarded:false` → POST → `true`; puppeteer click-through shots 13–16 in `evidence/hackathon-hour/` (budget → shops → starters → prefilled composer), all eyeballed.
- **Concurrent-session overlap:** the mobile-polish session's uncommitted hunks (styles.css 960-block rewrite + `.m-menu`, index.html `viewport-fit` + small edits) were in the shared working tree and rode along into `321e507` when those two files were git-added. Nothing lost — combined state is committed AND live-verified (my 1440px shots + that session's own desktop regression). Mobile session must NOT re-commit styles.css/index.html for that work; remaining dirty files (memory/*, wallet-control/*, watchdog.sh) belong to other sessions and were left alone. Lesson: on this shared repo, `git status` the exact paths immediately before `git add` and expect sibling sessions' hunks in hot files.

### wallet-control: confidence % + evidence-based user feedback (project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks)
<!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks -->
- Owner ask: "feedback to user should always be confidence percentage and evidence based… we wanna win this hackathon." Shipped across engine → worker → server API → web UI → tests.
- Engine (`lib/engine.js`, v1.3.0): new `conf` accounting — `cv()`/`co()` at ~16 check sites count deterministic checks that completed with readable data (verified) vs facts the engine could not verify (open: unreadable amount, missing velocity counter, absent merchant domain). Every customer-surfaced uncertainty counts as an open point too. Structured `confidence: {percent, verified_facts, open_points, method}` in the evaluation return; percent = round(100·verified/(verified+open)), capped at 99 — never claims certainty.
- Messages: all three branches (approve/decline/step_up) now state `N% confidence (v/t decision facts verified, o open)` and cite `Verified basis: <evidence labels>`; step-up lead re-punctuated to `— N% confidence` and open points sentence-ized so they don't run into the basis.
- Wiring: worker record + pushFeed carry `confidence`; server `/api/state` `pending_step_ups` carry it; web UI renders a color-banded badge (≥90 green / ≥70 amber / <70 red) on decision cards and step-up approval cards, next to the existing evidence grid. `#decisionBody` (sandbox API) deliberately UNCHANGED — documented contract, confidence rides inside `customer_message` instead.
- Verification: 6 new engine tests (message format, high-confidence on deterministic decline, gaps lower %, step-up counts open points, determinism, manipulation-note still formatted) → full suite 151 green incl. parallel sustainability suite (14); replay regression 45/45 decision lines byte-identical (timing-stripped); E2E smoke via booted server: SCEN0001 run produced 11 confidence objects in `/api/state`, pending step-up card carried its payload, messages read "92% confidence (11/12 decision facts verified, 1 open)".
- Commit-grouping lesson applied: worktree carried the sustainability session's finished work (lib/sustainability.js + tests + UI, suite green) — committed in separate grouped commits, secret-scanned data files first, exact-path `git status` before each `git add` per the 06:30 lesson.

## Wallet-control: sustainability preference + offer comparison (this session)

<!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks --> Added "🌱 Prefer sustainable shops" to wallet-control (advisory only, never touches engine decisions): static demo-grade scores in `wallet-control/data/sustainability.json` (14 CH shops; unknown shops = honest "unknown"), `lib/sustainability.js` (lookup + basic risk score from trusted list/threat-intel/Trusted Shops + rankOffers where sustainability breaks ties only WITHIN a risk band), server prefs persisted in `wallet-control/data/preferences.json`, `POST /api/settings/sustainability`, `POST /api/offers/compare`, sustainability field added to `/api/merchant/dossier` results; UI: masthead toggle + "Compare offers" box in panel 2. 14 new tests (`test/sustainability.test.js`, registered in npm test). Verified live: prefer=ON → decathlon.ch recommended among 4 shoe shops; OFF → pure risk order. NOT committed — shared files (server.js, web/*) carry other sessions' uncommitted WIP. Note: `test/engine.test.js` has 1 failing test ("step-up review states confidence…") from another session's in-flight confidence-accounting diff — not ours, left alone.

## Wallet-control: researched sustainability dataset for demo

<!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks --> Researched + rewrote `wallet-control/data/sustainability.json` (28 shops) on the Carpathia 2025 top-20 CH B2C ranking (FY2024: zalando 1.7bn, galaxus 1.65, digitec 1.15, amazon.de 910M, ricardo 800M, temu 700M, brack 570M…). Key researched facts in notes: Migros climate-neutral ops since 2021/−60% vs 2019; Coop dropped "klimaneutral" claims 2024; Zalando SBTi (−80% Scope 1+2 by 2025, ~2.54 Mt total); Amazon 80.9 Mt 2025 (+16%); Shein emissions nearly doubled 2023 (biggest fast-fashion polluter); Decathlon SBTi −42% Scope 1–3 by 2030; Bergfreunde climate-neutral since 2019 retroactive to 2006 + −60%; Ochsner Sport (largest CH sports retailer, 100 stores, PV) has NO published CO2 balance → scored 60; brack.ch scored DOWN to 58 (family-owned, no CO2 disclosure). Removed microspot.ch (offline since spring 2024) + melectronics.ch (post-MediaMarkt status unclear). Tests updated (brack 58). Live demo verified on port 8795 — NOTE: port 8794 is the other session's live shopper app; 8790 = wallet default; use 879x free ports for smoke tests.

## Wallet-control: behavioral quantity anomaly → user approval (500 shoes ≠ 500 gloves)

<!-- project: github-swiss-ai-weeks/itsmaxjeffrey/swiss-ai-weeks --> Owner ask: orders that are behaviorally strange (500 pairs of shoes) must raise risk and ask the user even at whitelisted merchants, while plausible bulk (500 disposable gloves) must pass, with the baseline self-adjusting to each user's history. Shipped in wallet-control: new `lib/item-classes.js` — category-conditional plausibility caps (bulk groceries/household 500, gift_card/subscriptions/membership 2, finite clothing/electronics/sporting_goods/cosmetics/books 12, services 20, unknown→finite conservative), adaptive via `max(baseCap, 3 × customer's observed per-category max)` from trained profile `qty_max_by_category`. Engine §6c `ITEM_QTY_ANOMALY`: over-cap line → uncertainty (routes via uncertainty_policy); severe excess (>3× cap) → `flags.behavior` forces step_up exactly like manipulation — works under `uncertainty_policy: approve` at trusted merchants (only `decline` suppresses). Behavior model v3: features 13 `log_item_qty_max` + 14 `qty_over_class_cap` — pack history has no item lines so both are zero-variance → weights exactly 0.0 (verified), scores/replay unchanged; parity vectors now carry `items` (PARITY_HUGE qty 500 electronics exercises both); profiles carry empty `qty_max_by_category` seam for future live quantity baselines (attempts were NOT used as baseline — fraud scenarios would self-whitelist). Verified: LOCO 0.7849 (v2 0.7850, float noise), attempt bands 28/15/2 unchanged, friction 3.02 %, engine 33/33 + behavior 14/14 + full npm test green, replay regression decision lines identical to pre-change baseline. EVAL.md carries the v3 round. Gotchas: (1) apply_patch multi-hunk failed atomically on a comment anchor that looked byte-identical (em-dashes/quotes) — the `edit` tool with exact pairs is more reliable for such files; (2) trained-quantity-features trick: constant training column × std floor 1e-8 → standardized 0 at inference for the observed value, and zero-init Adam keeps weight exactly 0, so new features are numerically inert until real variance exists — deterministic check enforces now, model learns later. Restart of any running wallet service needed to pick up the new engine code.
