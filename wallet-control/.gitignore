# derived from merchant-trust-data canonical artifact; loader falls back to it
lib/injection-model.json
lib/behavior-model.json
# runtime state
node_modules/

# saw26 sandbox key (plaintext fallback, owner-approved)
.saw26-key

# Optional TypeSafe credential file (prefer TYPESAFE_API_KEY_FILE outside repo)
.typesafe-key
.jev-key

# Private wallet signing authority and enrolled devices
.wallet-private/
data/.wallet-private/
